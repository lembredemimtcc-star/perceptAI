import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  menuButton: {
    marginLeft: 20,
  },

  titleContainer: {
    alignItems: "flex-start",
    paddingBottom: 5,
  },

  titleText: {
    fontSize: 18,
    fontWeight: "bold",
    color: "#000",
  },

  underline: {
    width: "100%",
    height: 2,
    backgroundColor: "#000",
    marginTop: 2,
  },
});
